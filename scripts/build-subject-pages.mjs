import {readFile,writeFile,mkdir} from "node:fs/promises";import path from "node:path";import {fileURLToPath} from "node:url";const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"..");const data=JSON.parse(await readFile(path.join(root,"data/hanlin-114.json"),"utf8"));const esc=v=>String(v).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"})[c]);for(const subject of data.subjects){const depth=subject.path.includes("/")?"../":"";const units=subject.units.map((u,i)=>`<article class="unit"><div class="num">${subject.id==="chinese"?`第 ${i+1} 課`:`第 ${i+1} ${subject.id==="life"?"主題":"單元"}`}</div><h3>${esc(u.title)}</h3><p>${esc(u.focus)}</p><button class="open" aria-expanded="false" aria-controls="detail-${esc(u.id)}">展開教學任務</button><div class="detail" id="detail-${esc(u.id)}" hidden><div class="mission"><strong>🎯 課堂任務</strong><br>${esc(u.mission)}</div><label class="check"><input type="checkbox"> 我能用自己的話說出這一課／單元的重點。</label></div></article>`).join("");const sources=data.sources.map(s=>`<li><a href="${esc(s.url)}" target="_blank" rel="noreferrer">${esc(s.label)}</a></li>`).join("");const html=`<!doctype html><html lang="zh-TW"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(subject.label)}教學駕駛艙｜翰林版小二上</title><link href="https://fonts.googleapis.com/css2?family=Noto+Sans+TC:wght@400;700;900&display=swap" rel="stylesheet"><link rel="stylesheet" href="${depth}assets/css/subject.css"><style>:root{--accent:${subject.accent}}</style></head><body><a class="skip" href="#content">跳到主要內容</a><header><div class="head"><a href="${depth}index.html">← 返回首頁</a><h1>${subject.emoji} ${esc(subject.label)}教學駕駛艙</h1><div class="meta"><span class="badge">翰林版</span><span class="badge">二年級上學期</span><span class="badge">114學年度目錄</span><span class="badge">原創教學活動</span></div></div></header><main id="content"><p class="intro">${esc(subject.intro)}</p><div class="tools" aria-label="課堂工具"><button id="font-up">A+ 放大</button><button id="font-down">A- 縮小</button><button id="expand-all">展開全部任務</button></div><h2>課程地圖</h2><div class="grid">${units}</div><aside class="sources"><strong>資料來源與使用邊界</strong><p>課名與單元依公開課程資料核對；本站不重製課文、習題或教師手冊。教學任務為本站原創，使用時請搭配合法教材。</p><ul>${sources}</ul></aside></main><footer>edu2 · 翰林版小二上 ${esc(subject.label)} · <a href="https://github.com/xwin20002/edu2">GitHub</a></footer><script src="${depth}assets/js/subject.js" defer></script></body></html>`;const target=path.join(root,subject.path);await mkdir(path.dirname(target),{recursive:true});await writeFile(target,html+"\n");console.log(`built ${subject.path}`);}
